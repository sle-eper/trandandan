version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: frontend/login_page/Dockerfile
    container_name: frontend
    ports:
      - "8080:80"
    volumes:
      - /frontend/login_page:/usr/share/nginx/html
    restart: always
